server:
  port: 8603

spring:
  application:
    name: mall-oms
  cloud:
    nacos:
      discovery:
        server-addr: http://c.youlai.store:8848
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml










