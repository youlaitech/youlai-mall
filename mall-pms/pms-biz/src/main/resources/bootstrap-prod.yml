server:
  port: 8602

spring:
  application:
    name: mall-pms
  cloud:
    nacos:
      discovery:
        server-addr:  http://c.youlai.store:8848
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml
