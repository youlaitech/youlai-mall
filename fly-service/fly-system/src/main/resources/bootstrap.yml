server:
  port: 8001

spring:
  application:
    name: fly-system
  cloud:
    config:
      uri: http://101.37.69.49:8888
      name: fly-system
      profile: dev
      label: master
#    bus:
#      refresh:
#        enabled: true
#  kafka:
#    bootstrap-servers: 101.37.69.49:9092
#  main:
#    allow-bean-definition-overriding: true



eureka:
  client:
    service-url:
      defaultZone: http://101.37.69.49:8761/eureka/
  instance:
    instance-id: 系统服务-8001
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 2
    lease-expiration-duration-in-seconds: 5

security:
  oauth2:
    resource:
      id: fly-user
      user-info-uri: http://localhost:9999/auth/user/current
      prefer-token-info: false

#swagger 配置
swagger:
  auth:
    server: http://101.37.69.49:8000/oauth/token  #验证token请求地址

management:
  endpoints:
    web:
      exposure:
        include: '*'