spring:
  port: 8000
  application:
    name: fly-auth
    main:
      allow-bean-definition-overriding: true
    cloud:
      config:
        uri: http://localhost:8888
        name: ${spring.application.name}
