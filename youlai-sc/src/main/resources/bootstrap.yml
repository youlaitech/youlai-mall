spring:
  application:
    name: youlai-sc
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      uri: http://localhost:8888
      name: ${spring.application.name}
oss:
  active: minio
  minio:
    endpoint: http://101.37.69.49:9000/
    access-key: minioadmin
    secret-key: minioadmin
    bucket-name: youlai-mall