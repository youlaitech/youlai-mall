server:
  port: 9999
spring:
  profiles:
    active: dev
  application:
    name: fly-gateway
  cloud:
    config:
      uri: http://101.37.69.49:8888
      name: ${spring.application.name}
      profile: ${spring.profiles.active}
